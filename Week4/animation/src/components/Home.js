import React from 'react';

const Home = () => {
  return (
    <div style={{
      height: '100vh',
      background: '#fff',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontSize: '2rem',
      fontWeight: 'bold'
    }}>
      🚀 Welcome to the Home Page!
    </div>
  );
};

export default Home;
